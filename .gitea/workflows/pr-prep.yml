name: OpenCode PR Prep
run-name: OpenCode PR Prep

"on":
  workflow_dispatch:
    inputs:
      pull_request_number:
        description: Pull request number to prepare for review
        required: true
        type: string
      head_sha:
        description: Expected pull request head SHA
        required: true
        type: string

permissions:
  contents: read

jobs:
  preflight:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      pull_request_number: ${{ steps.managed-config.outputs.pull_request_number }}
      head_sha: ${{ steps.managed-config.outputs.head_sha }}
      validation_supported: ${{ steps.managed-config.outputs.validation_supported }}
      playwright_smoke_supported: ${{ steps.managed-config.outputs.playwright_smoke_supported }}
      visual_validation_supported: ${{ steps.managed-config.outputs.visual_validation_supported }}
      controller_repository: ${{ steps.managed-config.outputs.controller_repository }}
      setup_branch: ${{ steps.managed-config.outputs.setup_branch }}
    env:
      GIT_SSL_NO_VERIFY: "1"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      OPENCODE_MANAGED_CONFIG: "project/opencode-managed.json"
      OPENCODE_REPO_OWNER: ${{ secrets.OPENCODE_REPO_OWNER }}
      OPENCODE_REPO_NAME: ${{ secrets.OPENCODE_REPO_NAME }}
      OPENCODE_CONTROLLER_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
      GITEA_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
    steps:
      - name: Allow internal self-signed Git over HTTPS
        run: git config --global http.sslVerify false

      - name: Check out default branch workflow definition
        run: |
          git clone --no-tags "https://review-bot:${GITEA_TOKEN}@git.ultramac.work/${OPENCODE_REPO_OWNER}/${OPENCODE_REPO_NAME}.git" .

      - name: Resolve managed PR-prep stages
        id: managed-config
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          event = json.loads(Path(os.environ["GITHUB_EVENT_PATH"]).read_text(encoding="utf-8"))
          dispatch_inputs = event.get("inputs") or event.get("workflow_dispatch", {}).get("inputs") or {}
          pull_request_number = str(dispatch_inputs.get("pull_request_number") or "").strip()
          head_sha = str(dispatch_inputs.get("head_sha") or "").strip()
          if not pull_request_number:
              raise SystemExit("workflow_dispatch input pull_request_number is required")
          if not head_sha:
              raise SystemExit("workflow_dispatch input head_sha is required")

          config = json.loads(Path("project/opencode-managed.json").read_text(encoding="utf-8"))
          assert config["schema_version"] == 1
          controller = config["controller"]
          assert controller["repository"]
          assert controller["setup_branch"]
          pr_prep = config["pr_prep"]
          assert pr_prep["workflow"] == ".gitea/workflows/pr-prep.yml"
          assert pr_prep["authoritative"] is True
          stages = pr_prep["stages"]
          assert "validation" in stages
          validation = config["validation"]
          assert validation["workflow"] == ".gitea/workflows/pr-tests.yml"
          assert validation["command"]
          playwright_smoke = config["playwright_smoke"]
          assert playwright_smoke["workflow"] == ".gitea/workflows/playwright-smoke.yml"
          assert playwright_smoke["command"]
          smoke_stage = (
              "playwright-smoke" in stages
              and playwright_smoke["supported"] is True
              and playwright_smoke["provider"] == "playwright"
              and playwright_smoke["platform"] == "web"
          )
          preview = config["preview"]
          visual_validation = config["visual_validation"]
          assert visual_validation["workflow"] == ".gitea/workflows/visual-validation.yml"
          visual_provider = str(visual_validation["provider"]).strip()
          visual_platform = str(visual_validation["platform"]).strip()
          visual_playwright_web = visual_provider == "playwright" and visual_platform == "web"
          visual_native_macos = visual_provider in {"native-macos", "native", "macos"} and visual_platform in {"macos", "native-macos"}
          visual_native_ios_simulator = visual_provider in {"native-ios-simulator", "ios-simulator", "native-ios", "ios"} and visual_platform in {"ios-simulator", "ios", "simulator"}
          visual_stage = (
              "visual-validation" in stages
              and visual_validation["supported"] is True
              and ((visual_playwright_web and preview["supported"] is True) or visual_native_macos or visual_native_ios_simulator)
          )
          with open(os.environ["GITHUB_OUTPUT"], "a", encoding="utf-8") as handle:
              handle.write(f"pull_request_number={pull_request_number}\n")
              handle.write(f"head_sha={head_sha}\n")
              handle.write("validation_supported=true\n")
              handle.write(f"playwright_smoke_supported={'true' if smoke_stage else 'false'}\n")
              handle.write(f"visual_validation_supported={'true' if visual_stage else 'false'}\n")
              handle.write(f"controller_repository={controller['repository']}\n")
              handle.write(f"setup_branch={controller['setup_branch']}\n")
          print("managed PR-prep config ok")
          PY

  validation:
    needs: preflight
    runs-on: macos-ultramac
    timeout-minutes: 10
    env:
      GIT_SSL_NO_VERIFY: "1"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      OPENCODE_MANAGED_CONFIG: "project/opencode-managed.json"
      OPENCODE_REPO_OWNER: ${{ secrets.OPENCODE_REPO_OWNER }}
      OPENCODE_REPO_NAME: ${{ secrets.OPENCODE_REPO_NAME }}
      GITEA_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
    steps:
      - name: Allow internal self-signed Git over HTTPS
        run: git config --global http.sslVerify false

      - name: Check out repository
        run: |
          git clone --no-tags "https://review-bot:${GITEA_TOKEN}@git.ultramac.work/${OPENCODE_REPO_OWNER}/${OPENCODE_REPO_NAME}.git" .

      - name: Resolve pull request dispatch inputs
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          event = json.loads(Path(os.environ["GITHUB_EVENT_PATH"]).read_text(encoding="utf-8"))
          dispatch_inputs = event.get("inputs") or event.get("workflow_dispatch", {}).get("inputs") or {}
          head_sha = str(dispatch_inputs.get("head_sha") or "").strip()
          if not head_sha:
              raise SystemExit("workflow_dispatch input head_sha is required")
          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as handle:
              handle.write(f"OPENCODE_HEAD_SHA={head_sha}\n")
          PY

      - name: Check out pull request head
        run: |
          git fetch --no-tags --prune --depth=1 origin "$OPENCODE_HEAD_SHA"
          git checkout --detach "$OPENCODE_HEAD_SHA"

      - name: Load managed validation settings
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          config = json.loads(Path("project/opencode-managed.json").read_text(encoding="utf-8"))
          validation = config["validation"]
          assert validation["command"]
          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as handle:
              handle.write(f"OPENCODE_VALIDATION_COMMAND={validation['command']}\n")
          PY

      - name: Run repository validation command
        run: /bin/bash -lc "$OPENCODE_VALIDATION_COMMAND"

  playwright-smoke:
    needs:
      - preflight
      - validation
    if: ${{ needs.preflight.outputs.playwright_smoke_supported == 'true' }}
    uses: ./.gitea/workflows/playwright-smoke.yml
    with:
      head_sha: ${{ needs.preflight.outputs.head_sha }}
    secrets: inherit

  visual-validation:
    needs:
      - preflight
      - validation
      - playwright-smoke
    if: ${{ always() && needs.preflight.result == 'success' && needs.validation.result == 'success' && (needs['playwright-smoke'].result == 'success' || needs['playwright-smoke'].result == 'skipped') && needs.preflight.outputs.visual_validation_supported == 'true' }}
    runs-on: macos-ultramac
    timeout-minutes: 15
    env:
      GIT_SSL_NO_VERIFY: "1"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      OPENCODE_MANAGED_CONFIG: "project/opencode-managed.json"
      OPENCODE_PLAYWRIGHT_EVIDENCE_RUNNER_IMAGE: "git.ultramac.work/eugene/opencode-gitea-automation/playwright-evidence-runner:1.54.0-r1"
      OPENCODE_REPO_OWNER: ${{ secrets.OPENCODE_REPO_OWNER }}
      OPENCODE_REPO_NAME: ${{ secrets.OPENCODE_REPO_NAME }}
      OPENCODE_CONTROLLER_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
      GITEA_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
    steps:
      - name: Allow internal self-signed Git over HTTPS
        run: git config --global http.sslVerify false

      - name: Check out repository
        run: |
          git clone --no-tags "https://review-bot:${GITEA_TOKEN}@git.ultramac.work/${OPENCODE_REPO_OWNER}/${OPENCODE_REPO_NAME}.git" .

      - name: Resolve pull request dispatch inputs
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          event = json.loads(Path(os.environ["GITHUB_EVENT_PATH"]).read_text(encoding="utf-8"))
          dispatch_inputs = event.get("inputs") or event.get("workflow_dispatch", {}).get("inputs") or {}
          pull_request_number = str(dispatch_inputs.get("pull_request_number") or "").strip()
          head_sha = str(dispatch_inputs.get("head_sha") or "").strip()
          if not pull_request_number:
              raise SystemExit("workflow_dispatch input pull_request_number is required")
          if not head_sha:
              raise SystemExit("workflow_dispatch input head_sha is required")
          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as handle:
              handle.write(f"OPENCODE_PULL_REQUEST_NUMBER={pull_request_number}\n")
              handle.write(f"OPENCODE_HEAD_SHA={head_sha}\n")
              config = json.loads(Path("project/opencode-managed.json").read_text(encoding="utf-8"))
              controller = config["controller"]
              handle.write(f"OPENCODE_CONTROLLER_REPOSITORY={controller['repository']}\n")
          PY

      - name: Check out pull request head
        run: |
          git fetch --no-tags --prune origin "$OPENCODE_HEAD_SHA"
          git checkout --detach "$OPENCODE_HEAD_SHA"

      - name: Load managed visual validation settings
        run: |
          python3 - <<'PY'
          import json
          import os
          import re
          import subprocess
          from pathlib import Path

          config = json.loads(Path("project/opencode-managed.json").read_text(encoding="utf-8"))
          assert config["schema_version"] == 1
          controller = config["controller"]
          assert controller["repository"]
          assert controller["setup_branch"]
          review = config["review"]
          assert review["persona"]
          assert review["persona_token_secret"].startswith("OPENCODE_PERSONA_")
          validation = config["validation"]
          assert validation["workflow"] == ".gitea/workflows/pr-tests.yml"
          preview = config["preview"]
          assert isinstance(preview["supported"], bool)
          visual_validation = config["visual_validation"]
          assert visual_validation["workflow"] == ".gitea/workflows/visual-validation.yml"
          assert isinstance(visual_validation["supported"], bool)
          assert visual_validation["provider"]
          assert visual_validation["platform"]
          assert visual_validation["full_page"] is True
          visual_provider = str(visual_validation["provider"]).strip()
          visual_platform = str(visual_validation["platform"]).strip()
          visual_playwright_web = visual_provider == "playwright" and visual_platform == "web"
          visual_native_macos = visual_provider in {"native-macos", "native", "macos"} and visual_platform in {"macos", "native-macos"}
          visual_native_ios_simulator = visual_provider in {"native-ios-simulator", "ios-simulator", "native-ios", "ios"} and visual_platform in {"ios-simulator", "ios", "simulator"}
          visual_evidence_supported = visual_validation["supported"] is True and (visual_playwright_web or visual_native_macos or visual_native_ios_simulator)
          owner = os.environ["OPENCODE_REPO_OWNER"].strip()
          repo = os.environ["OPENCODE_REPO_NAME"].strip()
          raw = f"{owner}-{repo}".lower()
          project_key = re.sub(r"[^a-z0-9]+", "-", raw).strip("-")
          project_key = re.sub(r"-{2,}", "-", project_key)
          if not project_key:
              raise SystemExit("Unable to derive project key from repository owner/name")
          default_branch = ""
          try:
              remote_head = subprocess.check_output(
                  ["git", "symbolic-ref", "--short", "refs/remotes/origin/HEAD"],
                  text=True,
              ).strip()
              if remote_head.startswith("origin/"):
                  default_branch = remote_head.removeprefix("origin/")
          except Exception:
              default_branch = ""
          if not default_branch:
              default_branch = os.environ.get("GITHUB_BASE_REF", "").strip() or "main"
          github_env = os.environ["GITHUB_ENV"]
          with open(github_env, "a", encoding="utf-8") as handle:
              handle.write(f"OPENCODE_CONTROLLER_REPOSITORY={controller['repository']}\n")
              handle.write(f"OPENCODE_SETUP_BRANCH={controller['setup_branch']}\n")
              handle.write(f"OPENCODE_PROJECT_KEY={project_key}\n")
              handle.write(f"OPENCODE_DEFAULT_BRANCH={default_branch}\n")
              handle.write(f"OPENCODE_PREVIEW_SUPPORTED={'true' if preview['supported'] else 'false'}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_SUPPORTED={'true' if visual_validation['supported'] else 'false'}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_PROVIDER={visual_provider}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_PLATFORM={visual_platform}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_EVIDENCE_SUPPORTED={'true' if visual_evidence_supported else 'false'}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_PLAYWRIGHT_WEB={'true' if visual_validation['supported'] is True and visual_playwright_web else 'false'}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_NATIVE_MACOS={'true' if visual_validation['supported'] is True and visual_native_macos else 'false'}\n")
              handle.write(f"OPENCODE_VISUAL_VALIDATION_NATIVE_IOS_SIMULATOR={'true' if visual_validation['supported'] is True and visual_native_ios_simulator else 'false'}\n")
          print("managed visual validation config ok")
          PY

      - name: Skip repositories without visual validation capture support
        if: ${{ env.OPENCODE_VISUAL_VALIDATION_EVIDENCE_SUPPORTED != 'true' }}
        run: echo "Managed repository does not declare supported visual validation capture; skipping."

      - name: Authenticate shared Playwright evidence runner image pull access
        if: ${{ env.OPENCODE_PREVIEW_SUPPORTED == 'true' && env.OPENCODE_VISUAL_VALIDATION_PLAYWRIGHT_WEB == 'true' }}
        run: echo "${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}" | docker login git.ultramac.work -u "review-bot" --password-stdin

      - name: Check out controller repository
        if: ${{ env.OPENCODE_VISUAL_VALIDATION_EVIDENCE_SUPPORTED == 'true' }}
        run: |
          git clone "https://review-bot:${GITEA_TOKEN}@git.ultramac.work/${OPENCODE_CONTROLLER_REPOSITORY}.git" controller

      - name: Resolve Python with working venv
        if: ${{ env.OPENCODE_VISUAL_VALIDATION_EVIDENCE_SUPPORTED == 'true' }}
        run: |
          HOST_HOME="$(eval echo "~$(whoami)")"
          echo "HOST_HOME=$HOST_HOME" >> "$GITHUB_ENV"
          echo "$HOST_HOME/.opencode/bin" >> "$GITHUB_PATH"
          echo "/opt/homebrew/bin" >> "$GITHUB_PATH"
          export PATH="$HOST_HOME/.opencode/bin:/opt/homebrew/bin:$PATH"
          source controller/scripts/lib/python_runtime.sh
          opencode_export_python_bin
          echo "PYTHON_BIN=$PYTHON_BIN" >> "$GITHUB_ENV"
          echo "VISUAL_VALIDATION_VENV=$PWD/.visual-validation-venv" >> "$GITHUB_ENV"

      - name: Install controller dependencies
        if: ${{ env.OPENCODE_VISUAL_VALIDATION_EVIDENCE_SUPPORTED == 'true' }}
        run: |
          "$PYTHON_BIN" --version
          "$PYTHON_BIN" -m venv "$VISUAL_VALIDATION_VENV"
          "$VISUAL_VALIDATION_VENV/bin/python" --version
          "$VISUAL_VALIDATION_VENV/bin/python" -m pip install -r controller/shared/requirements.txt

      - name: Configure live preview state access on host runners
        if: ${{ env.OPENCODE_PREVIEW_SUPPORTED == 'true' && env.OPENCODE_VISUAL_VALIDATION_PLAYWRIGHT_WEB == 'true' }}
        run: |
          if [[ -z "${OPENCODE_STATE_ROOT:-}" ]]; then
            DEFAULT_PREVIEW_STATE_ROOT="${HOST_HOME}/www/10-opencode-gitea-automation-state/preview"
            if [[ -d "${DEFAULT_PREVIEW_STATE_ROOT}" ]]; then
              echo "OPENCODE_STATE_ROOT=${DEFAULT_PREVIEW_STATE_ROOT}" >> "$GITHUB_ENV"
            fi
          fi

          LIVE_CONTROLLER_ROOT="${HOST_HOME}/www/10-opencode-gitea-automation"
          if [[ -d "${LIVE_CONTROLLER_ROOT}" ]]; then
            printf '%s\n' "OPENCODE_PREVIEW_ROUTER_RELOAD_COMMAND=/bin/zsh -lc 'cd ${LIVE_CONTROLLER_ROOT} && docker compose exec -T preview-router caddy reload --config /etc/caddy/Caddyfile --adapter caddyfile'" >> "$GITHUB_ENV"
          fi

      - name: Run visual validation capture worker
        if: ${{ env.OPENCODE_VISUAL_VALIDATION_EVIDENCE_SUPPORTED == 'true' }}
        run: |
          export HOME="$HOST_HOME"
          export PATH="$HOME/.opencode/bin:/opt/homebrew/bin:$PATH"
          cat > .opencode-visual-validation-project.json <<EOF
          {
            "project_key": "$OPENCODE_PROJECT_KEY",
            "repo_owner": "$OPENCODE_REPO_OWNER",
            "repo_name": "$OPENCODE_REPO_NAME",
            "default_branch": "$OPENCODE_DEFAULT_BRANCH"
          }
          EOF
          "$VISUAL_VALIDATION_VENV/bin/python" controller/scripts/visual_validation_worker.py \
            --repo-root "$PWD" \
            --config "$PWD/.opencode-visual-validation-project.json" \
            --automation-config "$PWD/controller/automation.json" \
            --event-name "workflow_dispatch" \
            --event-path "$GITHUB_EVENT_PATH"

  handoff:
    needs:
      - preflight
      - validation
      - playwright-smoke
      - visual-validation
    if: ${{ always() && needs.preflight.result == 'success' && needs.validation.result == 'success' && (needs['playwright-smoke'].result == 'success' || needs['playwright-smoke'].result == 'skipped') && (needs['visual-validation'].result == 'success' || needs['visual-validation'].result == 'skipped') }}
    runs-on: macos-ultramac
    timeout-minutes: 10
    env:
      GIT_SSL_NO_VERIFY: "1"
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
      PIP_DISABLE_PIP_VERSION_CHECK: "1"
      PYTHONDONTWRITEBYTECODE: "1"
      OPENCODE_MANAGED_CONFIG: "project/opencode-managed.json"
      OPENCODE_REPO_OWNER: ${{ secrets.OPENCODE_REPO_OWNER }}
      OPENCODE_REPO_NAME: ${{ secrets.OPENCODE_REPO_NAME }}
      OPENCODE_REVIEW_PERSONA: "review-bot"
      OPENCODE_REVIEW_PERSONA_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
      OPENCODE_CONTROLLER_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
      GITEA_TOKEN: ${{ secrets.OPENCODE_PERSONA_REVIEW_BOT_TOKEN }}
    steps:
      - name: Allow internal self-signed Git over HTTPS
        run: git config --global http.sslVerify false

      - name: Check out repository
        run: |
          git clone --no-tags "https://review-bot:${GITEA_TOKEN}@git.ultramac.work/${OPENCODE_REPO_OWNER}/${OPENCODE_REPO_NAME}.git" .

      - name: Resolve pull request dispatch inputs
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          event = json.loads(Path(os.environ["GITHUB_EVENT_PATH"]).read_text(encoding="utf-8"))
          dispatch_inputs = event.get("inputs") or event.get("workflow_dispatch", {}).get("inputs") or {}
          pull_request_number = str(dispatch_inputs.get("pull_request_number") or "").strip()
          head_sha = str(dispatch_inputs.get("head_sha") or "").strip()
          if not pull_request_number:
              raise SystemExit("workflow_dispatch input pull_request_number is required")
          if not head_sha:
              raise SystemExit("workflow_dispatch input head_sha is required")
          with open(os.environ["GITHUB_ENV"], "a", encoding="utf-8") as handle:
              handle.write(f"OPENCODE_PULL_REQUEST_NUMBER={pull_request_number}\n")
              handle.write(f"OPENCODE_HEAD_SHA={head_sha}\n")
              config = json.loads(Path("project/opencode-managed.json").read_text(encoding="utf-8"))
              controller = config["controller"]
              handle.write(f"OPENCODE_CONTROLLER_REPOSITORY={controller['repository']}\n")
          PY

      - name: Check out controller repository
        run: |
          git clone "https://review-bot:${GITEA_TOKEN}@git.ultramac.work/${OPENCODE_CONTROLLER_REPOSITORY}.git" controller

      - name: Resolve Python with working venv
        run: |
          echo "/opt/homebrew/bin" >> "$GITHUB_PATH"
          export PATH="/opt/homebrew/bin:$PATH"
          source controller/scripts/lib/python_runtime.sh
          opencode_export_python_bin
          echo "PYTHON_BIN=$PYTHON_BIN" >> "$GITHUB_ENV"
          echo "PR_PREP_VENV=$PWD/.pr-prep-venv" >> "$GITHUB_ENV"

      - name: Install controller dependencies
        run: |
          "$PYTHON_BIN" --version
          "$PYTHON_BIN" -m venv "$PR_PREP_VENV"
          "$PR_PREP_VENV/bin/python" --version
          "$PR_PREP_VENV/bin/python" -m pip install -r controller/shared/requirements.txt

      - name: Explicit review handoff
        run: |
          "$PR_PREP_VENV/bin/python" controller/scripts/pr_prep_worker.py \
            --repo-root "$PWD" \
            --automation-config "$PWD/controller/automation.json" \
            --managed-config "$PWD/project/opencode-managed.json" \
            --pull-request-number "$OPENCODE_PULL_REQUEST_NUMBER" \
            --head-sha "$OPENCODE_HEAD_SHA" \
            --handoff-review
